# Active Context — ShellGuard

## Current Work Focus

**🎯 IMMEDIATE: VitePress base-path defect fix — ✅ FIXED & TESTED.** Diagnosis confirmed at build-output level: Card.vue bound raw :href with no withBase(), so the 33 local card targets (all exist on disk) emitted root-absolute while the site deploys at /ShellGuard/ base — header/sidebar/hero worked because VitePress base-handles those classes. FIX: withBase() normalization in Card.vue (single choke point; siblings swept — zero raw hrefs). PROOF: rebuilt dist emits zero root-absolute section hrefs. GUARD: new tests/unit/docsLinks.test.ts (5 tests) covering all three link classes (config, markdown, raw HTML hrefs) + the withBase regression assertion. Gates: full oracle 16 files green, vite build clean. The third link class surfaced because the audit battery had only two — the battery grows every time it finds a blind spot.