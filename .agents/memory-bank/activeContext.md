# Active Context — ShellGuard
## Current Work Focus

Adopted `.agents/rules/project-hygiene.md` formalizing the end-to-end development protocol: Cartography & Grounding, Seam-Aware Planning, Layered Stroke Execution with incremental commits, Automated Verification Gates, and the mandatory human Live Verification Handshake. Aligned `cadence-and-lifecycle-prompts.md` Gate 1, and crystallized `the-seam-is-the-test-oracle` and `dual-verification-and-the-live-handshake` into long-term memory. Established the Focal Operating Matrix to systematically audit all user-facing seams.

## Recent Changes (Sliding Window — Latest 10)

1. **2026-09-21** — UI Action Seams Hardening & Seam Verification Test Suite: Inspected operational glue in `src/App.tsx`; explicitly preserved `tags`, `uris`, and `password_history` across `handleRenamePod`, `handleDeletePod`, `onBulkMoveToPod`, and `onBulkAssignTags`; wrapped all pod/bulk handlers and `ItemFormModal.onSave` in `try / catch` with reactive error reporting and safe `scuttleVault` resynchronization; created `tests/unit/uiSeams.test.ts` (11 unit tests covering endpoint dispatch routing, bulk deletion partitioning, selection state teardown, and metadata preservation). All automated gates green.
2. **2026-09-21** — Project Hygiene Protocol & Focal Seam Verification: Created `.agents/rules/project-hygiene.md` defining the 5-phase development lifecycle (Cartography, Seam-Aware Planning, Layered Stroke Commits, Automated Gates, and the Live Verification Handshake with Lucas). Aligned `cadence-and-lifecycle-prompts.md` Gate 1 so human operational testing in the browser precedes final commits. Crystallized `the-seam-is-the-test-oracle` and `dual-verification-and-the-live-handshake` into `.agents/memory-bank/long-term/`.
3. **2026-09-21** — Vault Item Deletion Parity & ConfirmDialog Integration: Resolved issue where vault items failed to delete despite user confirmation. Diagnosed and terminated zombie Node process holding port 6565 serving obsolete pre-Phase 21 routes; corrected `onDelete` and `onBulkDelete` item type routing in `src/App.tsx` to ensure `'totp'` and generic pearl records target `/api/vault` rather than `/api/attachments`; added Reef Modernist `ConfirmDialog` to single-item `ItemDetailPane.tsx` with reset on item change; cleared `selectedItemId` upon delete in `VaultShell.tsx`; added error handling and await for `scuttleVault`; created `tests/unit/vaultDelete.test.ts` (3 unit tests). All 25 test suites (288 passed, 1 skipped) pass 100% green.
4. **2026-09-20** — Peer Review Resolution, PBKDF2 Test Parity & Hardening — Phase 21 Sub-Phase [v0.0.2.3 / Build 25]: Resolved all findings from Cline's peer review reports: (C2) relocated sample exports to `tests/fixtures/bitwarden_sample.json` and `bitwarden_totp.json`, added `/bitwarden_export_*.json` to `.gitignore`, removed plaintext root copies; (C3) added `'uris'` to `METADATA_COLUMNS.vault_pearls` in `metadataGuard.ts` for Layer 2 AES-256-GCM metadata encryption; (C4 & C5 & S4) implemented pure-TS `pbkdf2Sha256` in `webCryptoFallback.ts`, branched KDF in `vaultExport.ts` (HKDF for ClawKey, PBKDF2 for passphrase), enforced CSPRNG (fail-closed, zero `Math.random`), added `v: 1`, `kdf`, and `kdfIterations` to envelope; (N1) added async native WebCrypto PBKDF2 fast-path (`crypto.subtle.deriveBits`) in `vaultExport.ts` to prevent main-thread UI freezing with pure-TS fallback for non-secure origins; (N2) updated `DEFAULT_PBKDF2_ITERATIONS` to 600,000 per OWASP guidance; (PBKDF2 Test Parity) added RFC 6070 known-answer vectors, `crypto.subtle.deriveBits` parity, and cross-origin HTTPS-to-LAN disaster-recovery tests in `webCryptoFallback.test.ts`; (I1) converted `0007.down.sql` to safe no-op (`SELECT 1;`); (I3, S2, S3) built dedicated RFC 6238 published test vector suite in `tests/unit/totpUtils.test.ts` (11 tests), rejected invalid Base32 input in `parseTotpSecret`, removed dead digits guard; (I2 & §4) synchronized full documentation audit across `ARCHITECTURE.md` (migrations 0001-0007, 24 suites, Delta #25), `project/database-schema.md`, `docs/reference/blueprint-schema.md`, root `CHANGELOG.md`, `skills/shellguard/SKILL.md`, `docs/agent-integration/api-reference.md`, `project/routes-and-contracts.md`, `project/shellcryption-spec.md`, `SECURITY.md`, `README.md`, `docs/vault-features/import-export.md`, `docs/vault-features/the-grotto.md`; and ignored tests/sqlite/wal in `vite.config.ts` watcher. 100% green test oracle across all 24 test suites (285 passed, 1 skipped), `tsc`, `vite build`, and `docs:build`.
5. **2026-09-20** — Phase 21 Shipped & Documented [v0.0.2.3 / Build 25, Tasks 41 & 42]: Delivered Task 41 (transactional `POST /api/vault/bulk-import` with 10MB scoped parser, per-record Zod validation, HTTP 207 Multi-Status reporting `{ inserted: string[], errors: [{ index, reason }] }`, and `DELETE /api/vault/bulk` with cascade attachment deletion and audit logging) and Task 42 (Reef Modernist batch selection in `VaultShell.tsx`, tri-state header checkbox, floating action bar guarded by `!isLocked`, bulk move to pod with zero tag loss, bulk tag assignment, custom modal confirmation dialogs replacing window.prompt/confirm, and error resolution chips in `ImportExportView.tsx`). Fixed critical route shadowing bug in `vault.ts` by registering bulk endpoints before parameterized `:id` routes. Built comprehensive test suite `tests/vault-bulk-import.test.ts` (11 tests). Verified 100% green across all 21 test suites (259 passed, 1 skipped), `tsc`, `vite build`, and `npm run docs:build`. Completed full documentation walk across root docs (`README.md`, `BLUEPRINT.md`) and `docs/vault-features/import-export.md`. Recorded commits `6f862e5` and `3b40008` cleanly.
6. **2026-09-19** — Phase 20: Vault Tagging System & Granular Filter Bar + 500MB Storage Ceiling + SSH Dual-Key Presentation [v0.0.2.2 / Build 24]: Delivered Tasks 39 & 40, Storage Ceiling sub-task, and SSH key dual-key ergonomics. Migration `0006_vault_tags.{up,down}.sql` adds `tags TEXT DEFAULT '[]'` column and owner index across `vault_pearls`, `vault_secure_notes`, and `vault_ssh_keys`. MetadataGuard registers `tags` for Layer 2 per-row metadata encryption. Server routes (`vault.ts`, `notes.ts`, `sshKeys.ts`) support `tags` on create/update and `?tags=a,b` intersection filtering with audit logging. Attachment ceiling raised from 50MB to 500MB (`ATTACHMENT_MAX_MB`) and grotto quota from 500MB to 1000MB (`GROTTO_QUOTA_MB`). Client UI gains `TagSelectorInput` autocomplete chips with color picker, unified color engine in `podUtils.ts` (string hashing + overrides), collapsible tag cloud in `SidebarFolderTree.tsx`, granular filter bar with `AND`/`OR` toggle in `ItemListPane.tsx`, and tag pill badges in `ItemDetailPane.tsx`. Enhanced SSH Key workflow: decoupled form input into clean PEM private key and OpenSSH public key fields, eliminated JSON leaks when unmasking private keys, added multi-line styled PEM code block rendering, instant **Download .pem**, **Copy Public Key**, and **Copy `authorized_keys` Command** (`echo "..." >> ~/.ssh/authorized_keys`). Verified 100% green across all 20 test suites (248 tests), `tsc`, `vite build`, and `docs:build`.
7. **2026-09-19** — Documentation-to-Code Parity Alignment & Phase 20 500MB Sub-task ("Docs Bow to Code"): Full project walk and audit resolved 12 contradictions across docs and UI: updated 8 locations citing obsolete 10MB base64 attachments to 50MB streaming BLOBs (with 500MB grotto quota), added missing `GET /api/attachments/:id/file` to README API table, unfroze ARCHITECTURE.md from v0.0.1.9 to v0.0.2.1 (added migration 0005, `attachmentBlobs.ts`, `backupManager.ts`, `requireAdmin.ts`, `attachmentUpload.ts`, updated test suites from 15 to 19 / 230 tests, added Deltas #21 & #22, corrected v0.3.0 phantom to Phase 13 v0.0.1.3), fixed rate limit conflation in architecture docs (10/15m auth, not 100/15m), synchronized blueprint-schema header (migrations 0001-0005), updated nginx proxy body size hint to 60M, and formalized Phase 20 sub-task elevating the per-file ceiling to 500MB in `ROADMAP.md` and `project/meta-prompt-ai-studio.md`. All 19 test suites (230 tests) and VitePress `docs:build` pass 100% green.
8. **2026-09-18** — Release v0.0.2.1 ("The Deep Storage Molt" — Phase 19, Build 22): Migrated binary attachment payloads from base64 text to native SQLite BLOB storage (`migrations/0005_attachment_blobs.{up,down}.sql` with in-code idempotent backfill `src/server/database/attachmentBlobs.ts`). Streamed wire contract: Busboy multipart POST with mid-stream 413 abort, metadata-only listing, chunked 1MB BLOB download via `GET /api/attachments/:id/file`. 50MB per-file ceiling (`ATTACHMENT_MAX_MB`) and 500MB per-owner quota (`GROTTO_QUOTA_MB`) both failing closed with 413. UI gains live upload progress with cancel, on-demand client decryption via shellKey, encrypted inline previews for images and PDFs (Blob object URLs), and folded-forward Eye-beside-Copy ergonomics across all masked field rows. Full oracle: 19 test files / 230 tests passing.
9. **2026-09-17** — Release v0.0.2.0 ("The Composite Reef" — Phase 18, Build 20): Consolidated vault items into primary rich composite records (passwords embed notes, live TOTP token with 30s countdown ring, attachments, custom fields). Decoupled child attachments from Pod item metrics so attached files never artificially inflate folder counts. Implemented in-browser WebCrypto SSH keypair engine (`src/lib/keyGen.ts` — Ed25519 + RSA-4096, ssh-keygen verified).
10. **2026-09-17** — CaraBase Brand Asset Alignment, Feature Graphic Restoration & README Sync: Redesigned ShellGuard brand assets to match CaraBase woodcut vector engraving aesthetic. Turned crab mascot body 180° forward/downward clasping the vault safe door with 3D 'S' crest. Replaced generic TOTP companion favicon re-use with a server-distinct notched carapace crest shield enclosing a multi-grid Web Globe in ShellGuard's signature purple/pink palette (`#e4048a`, `#ec4899`, `#c026d3`, `#ffffff`). Restored panoramic 1024x500 Web Feature Graphic banner. Twin parity verified across `public/` and `docs/public/assets/`.

## Active Decisions

- **Locked**: Dual Encrypted/Unencrypted Export Suite — ShellGuard AES-256-GCM encrypted envelope protects full vault with ClawKey or custom passphrase; RFC 4180 CSV export features configurable password inclusion toggle for sanitized catalog audits or full credential migrations.
- **Locked**: Dynamic RFC 6238 TOTP Engine (`totpUtils.ts`) — Pure-TS implementation supporting SHA1, SHA256, SHA512, 6 or 8 digits, custom periods (e.g. 15s/30s/60s), and bidirectional `otpauth://totp/` URI formatting.
- **Locked**: Universal Bitwarden Ingestion Pipeline (`bitwarden.ts`) — Folder-to-pod conversion via `normalizePod()`, compound SSH key serialization (`serializeSshKeySecret`), custom field translation (text, hidden, checkbox, linked), and explicit friendly error guidance on encrypted Bitwarden exports.
- **Locked**: Bulk Operations & 207 Multi-Status — `POST /api/vault/bulk-import` validates per-record within atomic SQLite transaction, returning 207 Multi-Status `{ inserted, errors }` on partial failure; `DELETE /api/vault/bulk` cascades linked attachments; floating action bar in `VaultShell.tsx` guarded by `!isLocked` and uses Reef Modernist custom modals (`ConfirmDialog` / inline inputs) with zero browser prompt/confirm.
- **Locked**: SSH Key Dual-Key Architecture — generated keypairs serialize into JSON `{ publicKey, privateKey }` sealed under Layer 1 ShellCryption in `key_value`; `parseSshKeySecret` provides transparent backward-compatibility for legacy raw PEMs; unmask toggle renders clean PKCS#8 PEM blocks with standard RFC 7468 delimiters.
- **Locked**: Vault Tagging Architecture — tags stored as JSON string arrays in `tags TEXT DEFAULT '[]'` across pearls, notes, and SSH keys. MetadataGuard encrypts tags on disk (Layer 2). List routes support `?tags=a,b` intersection.
- **Locked**: Unified Color Engine (`podUtils.ts`) — bioluminescent palette shared between Pods and Tags with deterministic string hashing (`hashStringToColor`) and explicit user overrides.
- **Locked**: Storage Ceiling Defaults — `ATTACHMENT_MAX_MB` default raised from 50MB to 500MB; `GROTTO_QUOTA_MB` default raised from 500MB to 1000MB (1GB).
- **Locked**: Agent memory bank isolation — Antigravity operates strictly in `.agents/memory-bank/`; Cline operates in `.clinerules/memory-bank/`. Never cross-touch or mirror.
- **Locked**: The ClawKey Canon — `ClawKey©™` (user identity key `hu-`), `ShellCryption©™` (client encryption engine), `LobsterKeys` (agent API keys `lb-`).
- **Locked**: Hash-only agent key ledger — migration `0004_key_ledger.sql` retired plaintext `api_key` column; hash storage only with constant-time verification.
- **Locked**: Attachment SQLite BLOB Storage & Streaming Wire Contract — migration `0005_attachment_blobs` rebuilds table with `file_data BLOB` + `size_bytes INTEGER`; Busboy multipart streaming POST, metadata-only list, 1MB chunked `substr` GET download.
- **Locked**: Bitwarden-style composite item model — passwords encapsulate embedded notes, live TOTP, attachments, custom fields; child attachments decoupled from pod counts.
- **Locked**: Eye-beside-Copy control layout — Unmask toggle (Eye/EyeOff) sits immediately LEFT of Copy in action cluster on all masked field rows (password, SSH key, hidden custom fields) with full-value mask invariant.
- **Locked**: Web server favicon identity — notched carapace crest shield enclosing multi-grid Web Globe in ShellGuard purple/pink palette (`#e4048a`, `#ec4899`, `#c026d3`, `#ffffff`).
- **Locked**: Asset twin parity — all brand assets must maintain 1:1 parity between `public/` and `docs/public/assets/`.
- **Locked**: SENSITIVE_KEY derived from `hu-` key via HKDF (one secret, one file).
- **Locked**: Keep SQLCipher whole-DB encryption as defense-in-depth alongside per-row encryption.
- **Locked**: SuperLobster Panel — no HTTP restore, no backup download, whitelist-only settings, strict-metadata user list, `audit.sqlite` never swapped by restore (ADMIN.md threat model).

## Important Patterns

- In-place encryption: no schema changes, encrypted JSON in same TEXT columns; binary attachment payloads stored as raw ciphertext BLOBs.
- Backward compatibility: legacy plaintext passes through on read; legacy attachment base64 rows backfilled to raw envelope BLOBs idempotently.
- `DB_ENCRYPTION_KEY` governs both SQLCipher AND per-row metadata encryption.
- Pure-TS cryptographic fallbacks (`webCryptoFallback.ts`) for AES-GCM-256 and HKDF-SHA256 ensure 100% test and LAN reliability without browser/node webcrypto hangs.
- Shared attachment helpers live in `src/lib/attachmentUtils.ts` (`uploadAttachmentMultipart`, `fetchAttachmentEnvelope`, `dataUrlToBlob`, `parseAttachmentIds`, `formatBytes`, `downloadAttachment`).

## Key Learnings

- Bitwarden export sniffer must check for encrypted exports before attempting standard item mapping to avoid creating encrypted gibberish items; explicit error prompts guide users to unencrypted JSON or CSV exports.
- Dynamic TOTP seeds from Bitwarden exports may arrive as either raw Base32 seeds or complete `otpauth://totp/` URIs with parameters (`digits`, `period`, `algorithm`); parsing into unified config objects preserves non-standard 8-digit and SHA256/SHA512 tokens.
- SQLite column addition migrations (`ALTER TABLE ADD COLUMN`) in `better-sqlite3` must use idempotent pragmas or error handling when ran in dynamic test suites to ensure clean re-runs.
- `better-sqlite3` exposes no native `openBlob()` streaming handle: the write path buffers ciphertext size up to the 50MB mid-stream ceiling; the download read path streams in 1MB chunks via `substr(file_data, offset, length)`.
- Encrypted inline previews on HTTP LAN: never use `data:` URIs (blocked on insecure origins in Chromium); convert decrypted payload to `Blob` and `URL.createObjectURL(blob)`, revoking on unmount/close.
- `crypto.randomUUID()` is strictly undefined in insecure browser contexts (e.g. non-localhost HTTP LAN IPs on Unraid) — client code must provide a resilient RFC 4122 v4 fallback using `crypto.getRandomValues()` / `Math.random`.
- Browser file downloads on HTTP origins: use `Blob([data], { type })` + `URL.createObjectURL(blob)` with cleanup `URL.revokeObjectURL(url)`.
- Node runtime binary on this host is `/config/Applications/node-v22.23.0-linux-x64/bin`. Workspace file permissions must belong to `abc:users` for test mkdtemp access.
