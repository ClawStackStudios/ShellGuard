# Active Context — ShellGuard
## Current Work Focus

Release v0.0.2.4 (Build 26) — Reef Polish Pass: Unified Search & Control Ergonomics. Phase 22 delivered, verified live in browser by Lucas on port 6464, and documented across the VitePress portal (`/vault-features/search`). Ready for main merge and release movement. Next forward phase in queue: Phase 23 (Bitwarden-Model Item Integrity — provisional v0.0.2.5 / Build 27).

## Recent Changes (Sliding Window — Latest 10)

1. **2026-09-23** — Phase 22 Documentation Walk & VitePress Search Portal Parity: Created dedicated VitePress documentation portal page `docs/vault-features/search.md` ("Unified Vault & Pod Search") explaining client-side zero-knowledge search mechanics (`vaultSearch.ts`), deep multi-field matching, in-memory lifecycle with lock purge, and the separation of concerns between Item List Pane search and Scoped Pod Tree filtering (`SidebarFolderTree.tsx`). Updated `docs/.vitepress/config.ts` sidebar, `docs/vault-features/index.md`, `docs/vault-features/the-grotto.md`, and `docs/reference/design-system.md`. Corrected `ARCHITECTURE.md` test suite inventory table from 23 to all 27 test suites sequential (323 passed, 1 skipped). Verified 100% green via `tests/unit/docsLinks.test.ts` (5/5), `tests/unit/mermaidDiagrams.test.ts` (5/5), `tsc --noEmit`, and `npm run docs:build` (93.93s).
2. **2026-09-23** — Release v0.0.2.4 ("The Reef Polish & Unified Ergonomics Molt" — Phase 22, Build 26): Delivered Tasks 43 & 44 on `feat/phase-22-unified-search`. Implemented pure client-side zero-knowledge search engine in `src/lib/vaultSearch.ts` querying in-memory decrypted items across titles, usernames, URLs, secondary URIs, decrypted notes, custom field names/values, attachment filenames, and tags, with automatic search state purge on lock/logout. Pruned 358 lines of redundant search markup by eliminating top-right Header search and top Sidebar search while preserving dedicated "Search Pods..." input in `SidebarFolderTree.tsx`. Verified Eye-beside-Copy ergonomics across all masked rows. Executed ClawStack Studios release protocol: bumped version to `v0.0.2.4 (Build 26)`, authored root `RELEASE-v0.0.2.4.md`, purged `RELEASE-v0.0.2.3.md`, archived Phase 19 (`v0.0.2.1`) into `.agents/memory-bank/ROADMAP-HISTORY.md`, updated `ROADMAP.md` (completion advanced to 84%), appended Delta #27 to `ARCHITECTURE.md`, and validated 100% green across all 27 test suites (323 passed, 1 skipped), `tsc`, `vite build`, and `docs:build`. Handshake verified live by Lucas on port 6464.
3. **2026-09-23** — Phase 22 Cartography & Pod Search Ergonomics Refinement: Mapped 4 search surfaces across the codebase (Header top-right, Sidebar top dashboard, SidebarFolderTree pod search, and ItemListPane vault list). Adopted Lucas's ergonomic refinement to retain the pod search in `SidebarFolderTree.tsx` for rapid pod navigation while removing redundant global search bars from `Header.tsx` and `Sidebar.tsx`. Established pure zero-knowledge `vaultSearch.ts` design on branch `feat/phase-22-unified-search`.
4. **2026-09-23** — Phase 25 Formalized (Habitat Full-Archive Mobility & Reactive Ingestion Dock — Tasks 49 & 50): Formalized Phase 25 at the queue summit across `ROADMAP.md` (queue advanced to 22 → 23 → 24 → 25, 80% completion across 25 phases) and `project/meta-prompt-ai-studio.md` (Stage 26 prompt transcribed). Delivers client-side collision-proof ZIP archive packaging with decrypted attachments (Task 49) and a Two-Stage Ingestion Pipeline (<2s instant bulk-import + floating non-blocking reactive Abyssal Ingestion Dock with live in-flight badge updates in `VaultShell` — Task 50), 1-upping industry standards.
5. **2026-09-22** — Release v0.0.2.3 ("The Deep Ingestion & Vault Parity Molt" — Phase 21, Build 25): Executed ClawStack Studios Release Protocol. Drafted root `RELEASE-v0.0.2.3.md` and purged previous draft `RELEASE-v0.0.2.2.md` maintaining Single Active Release Draft invariant. Executed Roadmap 3-version sliding-window protocol: retired Phase 18 (`v0.0.2.0 (Build 20)`) into `.agents/memory-bank/ROADMAP-HISTORY.md`, advanced `ROADMAP.md` current position to `v0.0.2.3 (Build 25) — released & live; next Phase 22 (provisional v0.0.2.4 / Build 26)`, and updated completion statistics to 83% (20 of 24 phases). Synchronized central version anchors across `package.json`, `package-lock.json`, `README.md`, `CHANGELOG.md`, and `ARCHITECTURE.md`. Automated verification gates 100% green across 26 test suites (313 passed, 1 skipped), `tsc --noEmit`, `vite build`, and `vitepress build docs`.
6. **2026-09-22** — Note Attachments Parity, Ghost Pod Purging, Detail Selection & Full Documentation Walk: Resolved 3 friction points surfaced in Lucas's live testing: (1) Purged ghost pod named 'Attachment' from pod selection chips and pod trees by filtering 'attachment' in getAllUniquePods and getStoredPodColors in src/lib/podUtils.ts and VaultShell.tsx; (2) Implemented complete binary attachment parity for secure notes via Migration 0008 (vault_secure_notes.attachments column), NoteSchemas create/update validation, notes.ts route handlers with cascade deletion of linked attachments, and App.tsx lockTheClaw/updateTheClaw attachment serialization; (3) Resolved detail pane blanking on save by separating uploadProgress and error banners from view-switching AnimatePresence and lifting selectedItemId to App.tsx with sticky selection on save. Updated label to 'Attachments (max 500MB per attachment)'. Completed comprehensive documentation walk across 16 files (ARCHITECTURE.md Delta #26, BLUEPRINT.md, CHANGELOG.md, README.md, ROADMAP.md, database-schema.md, blueprint-schema.md, meta-prompt-ai-studio.md, routes-and-contracts.md, SKILL.md, and docs/). All 26 test suites (313 passed), tsc lint, vite build, and docs:build 100% green.
7. **2026-09-21** — Live Verification Friction Points Resolution (Phase 21 Post-Verification Hardening): Resolved 4 core UX/security friction points identified during Lucas's comprehensive live verification: (Area 1: Account Switching & Modal Trap) Added `pendingSwitchTarget` and `isUnlockDismissed` in `src/App.tsx`; switching to a locked account keeps `activeLobsterId` intact so dismissing with 'X' never traps user; switching between unlocked accounts immediately purges in-memory vault items and resets folder/tags with zero page refresh; (Area 2: Hidden Custom Field Masking) Added eye/mask toggle buttons to both inline field builder and staged fields in `ItemFormModal.tsx` so secret values can be inspected before saving; (Area 3: Bulk Move & Tag Selection Chips) Replaced blank text inputs in bulk action modals with clickable pod chips (including root) and bioluminescent tag chips derived from vault items in `VaultShell.tsx`, with teardown effects preventing zombie selections; (Area 4: Binary Attachments & Grotto Overhaul) Added dedicated standalone dropzone in `ItemFormModal.tsx` auto-populating title from filename without password requirement; updated `lockTheClaw` to stream multipart upload with title/category overrides; updated `updateTheClaw` to merge existing and new attachment IDs without loss; removed inline preview modal and preview buttons from `ItemDetailPane.tsx` in favor of pure encrypted BLOB storage with direct download and delete. Extended `tests/unit/uiSeams.test.ts` (18 passed). Verified 100% green across `tsc --noEmit`, `vitest run` (306 passed), and `vite build`.
8. **2026-09-21** — UI Action Seams Hardening & Seam Verification Test Suite: Inspected operational glue in `src/App.tsx`; explicitly preserved `tags`, `uris`, and `password_history` across `handleRenamePod`, `handleDeletePod`, `onBulkMoveToPod`, and `onBulkAssignTags`; wrapped all pod/bulk handlers and `ItemFormModal.onSave` in `try / catch` with reactive error reporting and safe `scuttleVault` resynchronization; created `tests/unit/uiSeams.test.ts` (11 unit tests covering endpoint dispatch routing, bulk deletion partitioning, selection state teardown, and metadata preservation). All automated gates green.
9. **2026-09-21** — Project Hygiene Protocol & Focal Seam Verification: Created `.agents/rules/project-hygiene.md` defining the 5-phase development lifecycle (Cartography, Seam-Aware Planning, Layered Stroke Commits, Automated Gates, and the Live Verification Handshake with Lucas). Aligned `cadence-and-lifecycle-prompts.md` Gate 1 so human operational testing in the browser precedes final commits. Crystallized `the-seam-is-the-test-oracle` and `dual-verification-and-the-live-handshake` into `.agents/memory-bank/long-term/`.
10. **2026-09-21** — Vault Item Deletion Parity & ConfirmDialog Integration: Resolved issue where vault items failed to delete despite user confirmation. Diagnosed and terminated zombie Node process holding port 6565 serving obsolete pre-Phase 21 routes; corrected `onDelete` and `onBulkDelete` item type routing in `src/App.tsx` to ensure `'totp'` and generic pearl records target `/api/vault` rather than `/api/attachments`; added Reef Modernist `ConfirmDialog` to single-item `ItemDetailPane.tsx` with reset on item change; cleared `selectedItemId` upon delete in `VaultShell.tsx`; added error handling and await for `scuttleVault`; created `tests/unit/vaultDelete.test.ts` (3 unit tests). All 25 test suites (288 passed, 1 skipped) pass 100% green.

## Active Decisions

- **Locked**: Dual Encrypted/Unencrypted Export Suite — ShellGuard AES-256-GCM encrypted envelope protects full vault with ClawKey or custom passphrase; RFC 4180 CSV export features configurable password inclusion toggle for sanitized catalog audits or full credential migrations.
- **Locked**: Dynamic RFC 6238 TOTP Engine (`totpUtils.ts`) — Pure-TS implementation supporting SHA1, SHA256, SHA512, 6 or 8 digits, custom periods (e.g. 15s/30s/60s), and bidirectional `otpauth://totp/` URI formatting.
- **Locked**: Universal Bitwarden Ingestion Pipeline (`bitwarden.ts`) — Folder-to-pod conversion via `normalizePod()`, compound SSH key serialization (`serializeSshKeySecret`), custom field translation (text, hidden, checkbox, linked), and explicit friendly error guidance on encrypted Bitwarden exports.
- **Locked**: Bulk Operations & 207 Multi-Status — `POST /api/vault/bulk-import` validates per-record within atomic SQLite transaction, returning 207 Multi-Status `{ inserted, errors }` on partial failure; `DELETE /api/vault/bulk` cascades linked attachments; floating action bar in `VaultShell.tsx` guarded by `!isLocked` and uses Reef Modernist custom modals (`ConfirmDialog` / inline inputs) with zero browser prompt/confirm.
- **Locked**: SSH Key Dual-Key Architecture — generated keypairs serialize into JSON `{ publicKey, privateKey }` sealed under Layer 1 ShellCryption in `key_value`; `parseSshKeySecret` provides transparent backward-compatibility for legacy raw PEMs; unmask toggle renders clean PKCS#8 PEM blocks with standard RFC 7468 delimiters.
- **Locked**: Vault Tagging Architecture — tags stored as JSON string arrays in `tags TEXT DEFAULT '[]'` across pearls, notes, and SSH keys. MetadataGuard encrypts tags on disk (Layer 2). List routes support `?tags=a,b` intersection.
- **Locked**: Unified Color Engine (`podUtils.ts`) — bioluminescent palette shared between Pods and Tags with deterministic string hashing (`hashStringToColor`) and explicit user overrides.
- **Locked**: Storage Ceiling Defaults — `ATTACHMENT_MAX_MB` default raised from 50MB to 500MB; `GROTTO_QUOTA_MB` default raised from 500MB to 1000MB (1GB).
- **Locked**: Agent memory bank isolation — Antigravity operates strictly in `.agents/memory-bank/`; Cline operates in `.clinerules/memory-bank/`. Never cross-touch or mirror.
- **Locked**: The ClawKey Canon — `ClawKey©™` (user identity key `hu-`), `ShellCryption©™` (client encryption engine), `LobsterKeys` (agent API keys `lb-`).
- **Locked**: Hash-only agent key ledger — migration `0004_key_ledger.sql` retired plaintext `api_key` column; hash storage only with constant-time verification.
- **Locked**: Attachment SQLite BLOB Storage & Streaming Wire Contract — migration `0005_attachment_blobs` rebuilds table with `file_data BLOB` + `size_bytes INTEGER`; Busboy multipart streaming POST, metadata-only list, 1MB chunked `substr` GET download.
- **Locked**: Bitwarden-style composite item model — passwords encapsulate embedded notes, live TOTP, attachments, custom fields; child attachments decoupled from pod counts.
- **Locked**: Eye-beside-Copy control layout — Unmask toggle (Eye/EyeOff) sits immediately LEFT of Copy in action cluster on all masked field rows (password, SSH key, hidden custom fields) with full-value mask invariant.
- **Locked**: Web server favicon identity — notched carapace crest shield enclosing multi-grid Web Globe in ShellGuard purple/pink palette (`#e4048a`, `#ec4899`, `#c026d3`, `#ffffff`).
- **Locked**: Asset twin parity — all brand assets must maintain 1:1 parity between `public/` and `docs/public/assets/`.
- **Locked**: SENSITIVE_KEY derived from `hu-` key via HKDF (one secret, one file).
- **Locked**: Keep SQLCipher whole-DB encryption as defense-in-depth alongside per-row encryption.
- **Locked**: SuperLobster Panel — no HTTP restore, no backup download, whitelist-only settings, strict-metadata user list, `audit.sqlite` never swapped by restore (ADMIN.md threat model).

## Important Patterns

- In-place encryption: no schema changes, encrypted JSON in same TEXT columns; binary attachment payloads stored as raw ciphertext BLOBs.
- Backward compatibility: legacy plaintext passes through on read; legacy attachment base64 rows backfilled to raw envelope BLOBs idempotently.
- `DB_ENCRYPTION_KEY` governs both SQLCipher AND per-row metadata encryption.
- Pure-TS cryptographic fallbacks (`webCryptoFallback.ts`) for AES-GCM-256 and HKDF-SHA256 ensure 100% test and LAN reliability without browser/node webcrypto hangs.
- Shared attachment helpers live in `src/lib/attachmentUtils.ts` (`uploadAttachmentMultipart`, `fetchAttachmentEnvelope`, `dataUrlToBlob`, `parseAttachmentIds`, `formatBytes`, `downloadAttachment`).

## Key Learnings

- Bitwarden export sniffer must check for encrypted exports before attempting standard item mapping to avoid creating encrypted gibberish items; explicit error prompts guide users to unencrypted JSON or CSV exports.
- Dynamic TOTP seeds from Bitwarden exports may arrive as either raw Base32 seeds or complete `otpauth://totp/` URIs with parameters (`digits`, `period`, `algorithm`); parsing into unified config objects preserves non-standard 8-digit and SHA256/SHA512 tokens.
- SQLite column addition migrations (`ALTER TABLE ADD COLUMN`) in `better-sqlite3` must use idempotent pragmas or error handling when ran in dynamic test suites to ensure clean re-runs.
- `better-sqlite3` exposes no native `openBlob()` streaming handle: the write path buffers ciphertext size up to the 50MB mid-stream ceiling; the download read path streams in 1MB chunks via `substr(file_data, offset, length)`.
- Encrypted inline previews on HTTP LAN: never use `data:` URIs (blocked on insecure origins in Chromium); convert decrypted payload to `Blob` and `URL.createObjectURL(blob)`, revoking on unmount/close.
- `crypto.randomUUID()` is strictly undefined in insecure browser contexts (e.g. non-localhost HTTP LAN IPs on Unraid) — client code must provide a resilient RFC 4122 v4 fallback using `crypto.getRandomValues()` / `Math.random`.
- Browser file downloads on HTTP origins: use `Blob([data], { type })` + `URL.createObjectURL(blob)` with cleanup `URL.revokeObjectURL(url)`.
- Node runtime binary on this host is `/config/Applications/node-v22.23.0-linux-x64/bin`. Workspace file permissions must belong to `abc:users` for test mkdtemp access.
