# Active Context — ShellGuard

## Current Work Focus

**Phase 21 (Tasks 41/42) + Sub-Phases 21.1–21.3 IMPLEMENTED & COMMITTED (2026-09-20) — pending merge + release as v0.0.2.3 (Build 25)**. Complete and gate-verified on branch `feature/phase-21-bulk-operations-11309179680338905330` (commits `6f862e5` Tasks 41/42 · `3b40008` docs · `a72ce7d` sub-phase + 14-surface doc walk · `baab110` native PBKDF2 + 600k). **Tasks 41/42 delivered:** transactional `POST /api/vault/bulk-import` (scoped **10MB** body parser, per-record `VaultSchemas.bulkImportItem.safeParse()` validation, HTTP **207 Multi-Status** partial success `{ inserted: string[], errors: [{ index, reason }] }`, `type` constrained to `password|pearl`), `DELETE /api/vault/bulk` (`canDelete`, `{ ids: string[] }`, tx-wrapped attachment cascade + audit, 207 on missing IDs), tri-state multi-select + floating bulk action bar (`Move to Pod` / `Assign Tag` / `Delete`) in `VaultShell.tsx` guarded by `!isLocked`, Reef Modernist modals replacing native `prompt()`/`confirm()`, import preview table + error-resolution chips in `ImportExportView.tsx`, and the **P0 route-shadowing fix** (bulk routes now registered ABOVE the `:id` family). **Sub-Phases 21.1–21.3 delivered:** `src/lib/bitwarden.ts` (Bitwarden JSON/CSV ingestion — folders→pods, compound SSH keypairs, custom-field mapping, encrypted-export detection), `src/lib/totpUtils.ts` (dynamic RFC 6238 TOTP — SHA1/256/512, 6/8 digits, custom periods), migration `0007_composite_item_features` (`uris` Layer-2 encrypted + `password_history` sealed under the new `vault_pearls_history:{id}` AAD, safe no-op down), multi-URI rows + password-history drawer with one-click restore, and `src/lib/vaultExport.ts` (AES-256-GCM envelope v1 persisting `kdf`/`kdfIterations`, branched HKDF/PBKDF2 @600k, fail-closed CSPRNG). **Adversarially peer-reviewed across two rounds — 14 findings raised, all resolved** (route shadowing · `uris` plaintext · HKDF-as-passphrase-KDF · `Math.random()` nonce · root-level credential fixtures · destructive down-migration · stale docs/counts · missing TOTP suite → then main-thread PBKDF2 blocking · 100k iterations · the uncommitted-vs-`HEAD` gap). Gates: **24 test files / 282 passed / 1 skipped / 0 failed**, `tsc --noEmit`, `vite build` (~58s), `docs:build` (~98s). **Open:** PBKDF2 known-answer + native-vs-fallback parity test (the pure-TS branch is exercised by no suite). **NEXT: merge to `main` → cut v0.0.2.3 (Build 25, ships migration 0007) → Phase 22 (unified search).**

## Recent Changes (sliding window — 10)

1. **2026-09-20 — Phase 21 Sub-Phase landed & committed** (`a72ce7d` sub-phase source + 14-surface doc walk · `baab110` native PBKDF2 + 600k): Bitwarden JSON/CSV ingestion engine, dynamic RFC 6238 TOTP, migration `0007_composite_item_features` (`uris` + `password_history`), password-history drawer + multi-URI rows, dual export suite (envelope v1 + RFC 4180 CSV with sanitization toggle). 24 suites / 282 passed / 1 skipped.
2. **2026-09-20 — Adversarial peer review of the sub-phase: 14 findings over two rounds, all resolved.** Round 1 (11): root-level plaintext Bitwarden fixtures un-gitignored with tests coupled to `process.cwd()`; `uris` sent raw on all three write paths while `url` was Layer 2; HKDF misused as a passphrase KDF; `Math.random()` GCM nonce fallback; destructive `DROP COLUMN` down-migration; stale suite counts + missing deltas; no TOTP suite. Round 2 (3): **the fixes were all correct on disk and all absent from `HEAD`** (the commit-vs-checkout lesson); pure-JS PBKDF2 blocked the main thread; 100k iterations below OWASP. Also surfaced: `.clinerules/workflows/finish-task.md` instructs `git add .`, which swept another agent's in-flight work into a commit.
3. **2026-09-20 — Phase 21 Tasks 41/42 implemented & committed** (`6f862e5` + `3b40008`): bulk import/delete endpoints + tri-select UI + import wizard. Route-shadowing P0 fixed (bulk routes above `:id`).
4. **2026-09-19 — v0.0.2.2 "The Bioluminescent Reef" SHIPPED (Phase 20, Build 24)**: Vault tagging system (migration `0006_vault_tags`, MetadataGuard Layer 2 encryption, `?tags=a,b` intersection filtering with audit logging); 500MB attachment ceiling + 1000MB grotto quota; SSH dual-key architecture with clean PEM unmasking + terminal ergonomics. 20 test files, 248 tests passing (1 skipped). Release protocol: Build-sweep +1 (P21–P24 → Builds 25–28 incl. anchor hrefs), tag → push → GitHub Release mirror verified.
5. **2026-09-19 — Documentation-to-Code Parity Alignment (Phase 20 500MB sub-task)**: Full corpus walk resolved 12 contradictions: 8 obsolete "10 MB" refs → 500MB BLOB reality, missing `GET /api/attachments/:id/file` added to README API table, ARCHITECTURE unfrozen v0.0.1.9→v0.0.2.1, rate-limiter conflation fixed, blueprint-schema synced (0001–0006), nginx body hint → 60M. 20 suites / 248 tests + docs:build green.
6. **2026-09-18 — Release v0.0.2.1 "The Deep Storage Molt" SHIPPED (Phase 19, Build 22)**: BLOB migration (0005 + idempotent backfill), Busboy streaming, 50MB/500MB limits, Eye-beside-Copy folded from P22/T44.
7. **2026-09-18 — Release protocol executed** (v0.0.2.0): ROADMAP-HISTORY (P15), Completed Releases receipts (`6f9b00d`/`61336a1`), build labels swept +1 (P19–P24 → 22–27 incl. anchors), spine Stage 19 → SHIPPED.
8. **2026-09-17 — README TOTP-style reorg** (`96e6047`): 17-bullet wall → 4 grouped sub-headings, merged encryption sections, 3 callouts → 1 CAUTION; 16/16 anchors resolve.
9. **2026-09-17 — Mermaid rendering fixed** (`ccdfd55`): `vitepress-plugin-mermaid` + `withMermaid()`; lesson: an audit has one blind spot per unexamined class.
10. **2026-09-16 — Long-Term Memory Bank initialized** (`0a2c16c`/`bb9b95c`): `long-term/{patterns,decisions,learnings,constraints}.md` + `auditPerspective.md`. Territory split: `.clinerules/` = Cline; `.agents/` = Antigravity.
## Active Decisions & Considerations

- **Docs bow to code** (ratified): contradicting docs are the defect; the application works and is secure.
- **Honest PATCH over milestone theft**: doc-only/architecture sessions ship as PATCH; MINOR versions reserved for real feature phases.
- **2-Task Pairing Law + 📚 Documentation Impact** as definition-of-done for every phase.
- **Phase 21 — per-record `safeParse` over middleware `validateBody`**: `validateBody` rejects the whole payload with 400 on first failure, which is incompatible with 207 partial success. The container schema validates array bounds (`1..1000`) only; each record goes through `VaultSchemas.bulkImportItem.safeParse()` inside the route so failures aggregate into `errors[{index, reason}]` while valid records persist. Deliberate — do not "fix" back to a fully-typed array schema.
- **Phase 21 — bulk routes MUST register above the `:id` family**: `DELETE /api/vault/bulk` was shadowed by `router.delete('/:id')` (matched `id="bulk"` → 404). Bulk routes now sit above `/:id`; keep this ordering invariant for any future literal-path routes.
- **Phase 21 — bulk delete verb**: `DELETE /api/vault/bulk` (not POST), gated `requirePermission('canDelete')` per the locked verb→permission map.
- **Phase 21 — scoped 10MB parser**: `app.use('/api/vault/bulk-import', express.json({ limit: '10mb' }))` mounted before the 1MB global parser; supports up to 1000 items with custom fields.
- **Phase 21 sub-phase — `vault_pearls_history:{id}` AAD namespace**: password history is sealed client-side exactly like `secret`/`totp_secret`/`custom_fields`, and is deliberately **NOT** registered in `metadataGuard` (registering it would double-encrypt). New namespace — added to the AAD canon in `project/shellcryption-spec.md` + `project/encryption-layers-spec.md`.
- **Phase 21 sub-phase — `uris` is metadata, not ciphertext**: secondary login URIs go through **Layer 2** in `metadataGuard` (symmetric with the primary `url`). Contrast with `password_history`, which is Layer 1 — the two new 0007 columns sit in *different* encryption layers, deliberately.
- **Phase 21 sub-phase — PBKDF2 dual-path**: `deriveKeyForEnvelope` uses native `crypto.subtle.deriveBits` on secure origins and pure-TS `pbkdf2Sha256` on plain-HTTP LAN; **600,000 iterations** (OWASP) for passphrases, HKDF for the high-entropy ClawKey. The envelope persists `kdf` + `kdfIterations`, so raising the count never breaks existing backups. ⚠️ The two derivation paths are a **portability contract** and are currently untested for parity.
- **Phase 21 sub-phase — crypto fails closed**: the export **throws** when `crypto.getRandomValues` is unavailable rather than degrading the GCM nonce. Never reintroduce a `Math.random()` fallback for key material or nonces.
- **Phase 21 sub-phase — migration 0007 down is a no-op**: rolling back added columns must never drop user data; `SELECT 1;` matches the 0002/0003 precedent.
- **Phase 20 attachment ceiling escalation**: 50MB → 500MB file ceiling, 500MB → 1000MB grotto quota (env-tunable, 413 fail-closed, Busboy mid-stream abort).
- **SSH dual-key architecture**: `parseSshKeySecret`/`serializeSshKeySecret` envelope with backward compat for legacy raw PEMs; strict RFC 7468 PKCS#8 framing prevents JSON unmasking leaks.
- **PodUtils color unification**: shared `hashStringToColor` engine for pods AND tags; Node test-env-safe (`typeof localStorage === 'undefined'` guards).
- Phase 23 (attachment parent enforcement) is where standalone attachments are removed — NOT Phase 19/20/21.

## Next Steps & Pending Items

1. **Merge + release Phase 21 and its sub-phases**: merge `feature/phase-21-bulk-operations-11309179680338905330` → `main`; cut **v0.0.2.3 (Build 25)** per the release protocol (version bump, RELEASE doc roll, Build-sweep +1: P22–P24 → Builds 26–28 **incl. anchor hrefs**, tag → push → mirror verify). **This release ships migration `0007_composite_item_features`** — call it out in the release notes since containers run migrations at boot. ROADMAP Tasks 41/42 are `[x]` but **sub-phases 21.1–21.3 are still `[ ]`** and `current_position` still reads "next Phase 21" — both need the sweep.
2. **Close the PBKDF2 parity gap** (in flight with the implementing agent): add a known-answer vector (RFC 8018, or Node `crypto.pbkdf2Sync` as oracle — matching how `webCryptoFallback.test.ts` already validates SHA/HMAC/HKDF "compared to Node crypto") **and** a native-vs-fallback parity assertion. The pure-TS branch currently runs in no suite.
3. **Fix `workflows/finish-task.md`** — it instructs `git add .`, which swept another agent's in-flight `.clinerules/` work into an unrelated commit. Should stage by explicit path with `git diff --cached --stat` as the gate.
4. **Phase 22 (Reef Polish Pass — Unified Search)**: one search surface (client-side, zero-knowledge over decrypted corpus matching titles/keywords/attachment names/note contents/custom fields); remove header + sidebar search; verify Eye-beside-Copy ergonomics (folded into P19/T38 — verify-only).
5. **Phase 23**: Attachment parent enforcement (reject standalone attachments, orphan quarantine); notes reject password payloads; type-truthful dashboard.
6. **Phase 24**: WebCrypto fallback vector parity (unskip test), mechanized constant-time sweep in CI, claim battery gate script, threat-model addendum.
7. **Governance note**: `.agents/memory-bank/` is Antigravity's territory; `.jules/` is a third agent identity. `.clinerules/` remains my only bank.
